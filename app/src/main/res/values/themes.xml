<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base: la UI vera e' in Compose (Material 3). -->
    <style name="Theme.DashboardAlert" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
